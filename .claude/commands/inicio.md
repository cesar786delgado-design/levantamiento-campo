Lee `CLAUDE.md` y `MEMORY.md` completos antes de hacer nada más.

Dame un reporte corto (sin ceremonia) con:

1. **Dónde nos quedamos** — resumen de 2-3 líneas de la sección "Dónde nos
   quedamos" de MEMORY.md.
2. **Pendientes activos** — la lista tal cual está en MEMORY.md.
3. **Última verificación de estado real:** corre `git log --oneline -5` y
   `git status` y compáralo contra lo que dice MEMORY.md — si hay
   discrepancia (commits que MEMORY.md no menciona, cambios sin commitear
   que no se esperaban), dilo explícitamente antes de continuar.
4. Pregúntame en qué vamos a trabajar hoy. No empieces a modificar código
   todavía.

No corras nada en Supabase directo. No hagas commits en este paso — es
solo de lectura y reporte.
